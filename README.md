# Mac Setup #

Scripts to bootstrap a new mac with my flavour of apps and config.

# Install #

```
curl -fsSL https://github.com/adamtaylor/mac-setup/archive/refs/heads/main.zip -o mac-setup.zip \
    && unzip mac-setup.zip -d mac-setup \
    && rm mac-setup.zip
```
